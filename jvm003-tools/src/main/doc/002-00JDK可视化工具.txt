》JDK可视化工具

    Jconsole:
        概览：

        内存：相当于jstat命令，用于监视收集器管理的虚拟内存（Java堆，和永久代）的变化趋势

        线程：相当于jstack命令，遇到线程停顿的时候可以使用这个标签。
             等待外部资源（数据库，网络，设备资源等）
             死循环
             锁等待（活锁，死锁）

        类：

        VM概要：

        MBean: